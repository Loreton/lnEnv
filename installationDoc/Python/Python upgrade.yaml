#  by Loreto:  05-01-2021 15.10.16

https://indomus.it/guide/aggiornare-python-su-raspbian-alla-3-8-x-o-superiori-e-ripristinare-home-assistant-core/

check if 32 or 64 bit
    python -c 'import struct;print(8*struct.calcsize("P"))'


    sudo apt update
    sudo apt upgrade
    sudo apt-get install build-essential tk-dev libncurses5-dev libncursesw5-dev libreadline6-dev libdb5.3-dev libgdbm-dev libsqlite3-dev libssl-dev libbz2-dev libexpat1-dev liblzma-dev zlib1g-dev libopenjp2-7 libtiff5 libffi-dev libjpeg-dev bluetooth libbluetooth-dev -y
    sudo pip3 install pybluez



    # https://www.activestate.com/resources/quick-reads/how-to-update-all-python-packages/

    # list of all installed packages:
        pip3 freeze
        pip list --outdated

    # upgrade all python packages
        pip3 list --outdated --format=freeze | grep -v '^\-e' | cut -d = -f 1 | xargs -n1 pip3 install -U

    # ... oppure
    # Output a list of installed packages into a requirements file (requirements.txt):
        pip3 freeze > requirements.txt
    # Edit requirements.txt, and replace all ‘==’ with ‘>=’. Use the ‘Replace All’ command in the editor.

    # Upgrade all outdated packages:
        pip3 install -r requirements.txt --upgrade


    py38:
        wget https://www.python.org/ftp/python/3.8.6/Python-3.8.6.tgz
        tar xzvf Python-3.8.6.tgz
        cd Python-3.8.6/
    py39:
        VPY='3.9.5'
        wget https://www.python.org/ftp/python/${VPY}/Python-${VPY}.tgz
        # wget https://www.python.org/ftp/python/3.9.1/Python-3.9.5.tgz
        tar xzvf Python-${VPY}.tgz
        cd Python-${VPY}/

    ./configure --enable-optimizations --enable-loadable-sqlite-extensions
    make -j 8
    sudo make install

    please see: "..\Linux\Alternatives.yaml"

sudo apt-get update
sudo apt-get update --fix-missing
sudo apt install python-setuptools
sudo apt install python3-pip
