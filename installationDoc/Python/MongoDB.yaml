OLD: 

    https://docs.microsoft.com/en-us/windows/python/databases
        sudo apt update
        sudo apt-get install mongodb (server)
        sudo -H pip3 install pymongo

    There are 3 commands you need to know once MongoDB is installed:
        sudo service mongodb status
        sudo service mongodb start
        sudo service mongodb stop

NEW:
    https://developer.mongodb.com/how-to/mongodb-on-raspberry-pi/
        # Install the MongoDB 4.4 GPG key:
        wget -qO - https://www.mongodb.org/static/pgp/server-4.4.asc | sudo apt-key add -

        # Add the source location for the MongoDB packages:
        echo "deb [ arch=amd64,arm64 ] https://repo.mongodb.org/apt/ubuntu focal/mongodb-org/4.4 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-4.4.list

        # Download the package details for the MongoDB packages:
        sudo apt-get update

        # Install MongoDB:
        sudo apt-get install -y mongodb-org


        # Ensure mongod config is picked up:
        sudo systemctl daemon-reload

        # Tell systemd to run mongod on reboot:
        sudo systemctl enable mongod

        # Start up mongod!
        sudo systemctl start mongod
        sudo systemctl status mongod


import:

mongoimport  --host 127.0.0.1:27017 --db Ln_eBooks --collection ePubs --file /mnt/k/Download/mongo_exported/2020-05-01_Dictionary_noArray.json
mongoimport  --host 127.0.0.1:27017 --db Ln_eBooks --collection ePubs --file /mnt/k/Download/mongo_exported/2020-05-01_ePubs_noArray.json