[project]
name = "lnEnv"
version = "0.0.1"
description = "Modern uv-based Python project"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]

[tool.ruff]
line-length = 100

[build-system]
requires = [
    "setuptools",
]
build-backend = "setuptools.build_meta"
